---
layout: ../../layout.astro
title: setup()
description: Typed machine factory.
---

# `setup()`

```ts
import { setup } from '@chimba-ui/state-machine'
```

> TODO: how `setup()` binds context/event/implementation types so `machine()`
> calls are fully inferred.
