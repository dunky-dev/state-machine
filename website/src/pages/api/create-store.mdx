---
layout: ../../layout.astro
title: createStore()
description: The store primitive.
---

# `createStore()`

```ts
import { createStore } from '@chimba-ui/state-machine'
```

> TODO: the reactive store behind machines. Related types: `Store`, `Listener`,
> `SetStateAction`.
