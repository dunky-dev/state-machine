---
layout: ../../layout.astro
title: compose()
description: Combine machines.
---

# `compose()`

```ts
import { compose } from '@chimba-ui/state-machine'
```

> TODO: how to combine machines into a composition. Related type: `Composition`.
