---
layout: ../../layout.astro
title: Actions
description: Declare transition actions.
---

# Actions

```ts
import { act, oneOf } from '@chimba-ui/state-machine'
```

> TODO: signatures and examples for `act` and `oneOf`. Related types: `Action`,
> `ActionArg`, `ActionParams`, `OneOf`.
