---
layout: ../../layout.astro
title: connector()
description: Turn machine state into agnostic bindings.
---

# `connector()`

```ts
import { connector } from '@chimba-ui/state-machine'
```

> TODO: how the connector maps state to bindings and stays in sync. Related types:
> `Connect`, `Connector`, `ConnectSnapshot`, `Reaction`.
